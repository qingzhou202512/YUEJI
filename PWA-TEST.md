# PWA 功能测试指南

## 快速测试步骤

### 1. 构建项目

```bash
npm run build
npm run preview
```

### 2. 在浏览器中测试

#### Chrome/Edge

1. 打开 `http://localhost:4173`（preview 默认端口）
2. 打开 Chrome DevTools (F12)
3. 进入 **Application** 标签页

#### 检查清单

**Manifest**
- 进入 **Application > Manifest**
- 检查是否显示应用信息
- 确认图标路径正确（如果已添加图标）

**Service Worker**
- 进入 **Application > Service Workers**
- 应该看到 `service-worker.js` 已注册
- 状态应该是 "activated and is running"

**离线测试**
- 进入 **Network** 标签页
- 勾选 **Offline** 选项
- 刷新页面，应该仍能正常显示（使用缓存）

**安装测试**
- 在地址栏右侧应该看到安装图标（📱）
- 或者进入 **Application > Manifest**，点击 **Add to homescreen**

### 3. 移动端测试

#### Android Chrome

1. 在手机上访问应用地址
2. 浏览器会显示"添加到主屏幕"提示
3. 或通过菜单 > "添加到主屏幕"
4. 安装后，应用会像原生应用一样运行

#### iOS Safari

1. 在 Safari 中打开应用
2. 点击分享按钮（底部中间）
3. 选择"添加到主屏幕"
4. 安装后，应用会以独立模式运行

## 功能验证

### ✅ 离线访问
- [ ] 断开网络后，应用仍能打开
- [ ] 可以查看已保存的记录
- [ ] 显示"离线模式"提示

### ✅ 安装功能
- [ ] 显示安装提示（首次访问）
- [ ] 可以成功安装到主屏幕
- [ ] 安装后以独立窗口运行

### ✅ 数据持久化
- [ ] 离线时保存的记录不会丢失
- [ ] 重新联网后数据正常显示

## 常见问题

### Service Worker 未注册

**原因**：
- 不是 HTTPS 或 localhost
- Service Worker 文件路径错误

**解决**：
- 确保在 localhost 或 HTTPS 环境下运行
- 检查 `public/service-worker.js` 文件是否存在

### 安装提示不显示

**原因**：
- 应用已安装
- 浏览器不支持 PWA
- Manifest 配置错误

**解决**：
- 检查是否已安装（Application > Manifest）
- 使用支持的浏览器（Chrome、Edge、Safari）
- 检查 manifest.json 格式是否正确

### 离线时无法访问

**原因**：
- Service Worker 未正确缓存资源
- 缓存策略有问题

**解决**：
- 检查 Service Worker 日志（Console）
- 确认核心资源已被缓存
- 检查 Network 标签页的缓存状态

## 调试技巧

### 查看 Service Worker 日志

在 Console 中查看：
```
[Service Worker] 安装中...
[Service Worker] 激活中...
[PWA] Service Worker 注册成功
```

### 清除缓存重新测试

1. 进入 **Application > Storage**
2. 点击 **Clear site data**
3. 刷新页面重新测试

### 强制更新 Service Worker

1. 进入 **Application > Service Workers**
2. 点击 **Unregister**
3. 刷新页面重新注册




